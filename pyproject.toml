[project]
name = "bayesflow-xai"
version = "0.1.0"
description = "Explainable AI (Integrated Gradients, Saliency Maps, Attention Rollout, Masking) for BayesFlow simulation-based inference models: SIR, Lotka-Volterra, and Gaussian Random Field"
requires-python = ">=3.10"
dependencies = [
    "bayesflow",
    "keras>=3",
    "torch",
    "captum",
    "umap-learn",
    "scikit-learn",
    "scipy",
    "matplotlib",
    "pandas",
    "numpy",
    "FyeldGenerator",
    "pyyaml",
]

[project.optional-dependencies]
dev = ["pytest", "black", "ruff"]

[tool.setuptools.packages.find]
where = ["src"]
