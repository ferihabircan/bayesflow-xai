# diagnostics

Thin wrappers around BayesFlow's built-in health-check plots: loss
trajectory, parameter recovery, and simulation-based calibration (SBC)
histogram. `run_all_diagnostics(workflow, history)` runs all three and
saves them to `outputs/figures/`.
